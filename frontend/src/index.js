import React from 'react';  // React em si
import ReactDOM from 'react-dom';   // Integração do react em browsers
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

